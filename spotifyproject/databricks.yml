# This is a Databricks asset bundle definition for spotifyproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotifyproject
  uuid: 5b55c0df-abe8-449e-8241-b01afe32a1e0

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3240839050375465.5.azuredatabricks.net
      root_path: /Workspace/Users/yashwanthsaideep_outlook.com#ext#@yashwanthsaideepoutlook.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
      presets:
        source_linked_deployement: false
        
    variables:
      catalog: databricks_final_spotify
      schema: prod
 
  prod:
    mode: production
    workspace:
      host: https://adb-3240839050375465.5.azuredatabricks.net
      root_path: /Workspace/PROD.bundle/${bundle.name}/${bundle.target}
      presets:
        source_linked_deployement: false
      

    variables:
      catalog: databricks_final_spotify
      schema: prod
      
    permissions:
      - user_name: yashwanthsaideep_outlook.com#ext#@yashwanthsaideepoutlook.onmicrosoft.com
        level: CAN_MANAGE
